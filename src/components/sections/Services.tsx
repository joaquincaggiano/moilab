import { onScroll, animate } from "animejs";

const Services = () => {
  return (
    <section className="w-full h-screen" id="services">
      <div className="container mx-auto px-4 py-40 flex flex-col">
        

      </div>
    </section>
  );
};

export default Services;
